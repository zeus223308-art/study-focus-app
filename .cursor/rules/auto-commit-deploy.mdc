---
description: (See agent-automation.mdc) Commit, push, deploy after every change
alwaysApply: true
---

# Auto commit, push, deploy

**Full policy:** `.cursor/rules/agent-automation.mdc` (always apply).

Summary: after any code change → commit → push `main` → wait for **Deploy Web** (background) → tell user hash + https://zeus223308-art.github.io/study-focus-app/

Workflow: `.github/workflows/deploy-web.yml`
