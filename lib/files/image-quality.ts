/** Bump when on-device derivative regeneration rules change. */
export const ASSET_QUALITY_VERSION = 2;

/** Camera / picker — max JPEG quality (0–1). */
export const IMAGE_CAPTURE_QUALITY = 1;

/** Full-resolution master stored on device and in Drive backup. */
export const MASTER_MAX_EDGE = 2560;
export const MASTER_JPEG_QUALITY = 0.92;

/** Folder grid / list thumbnails. */
export const THUMB_MAX_EDGE = 960;
export const THUMB_JPEG_QUALITY = 0.88;

/** In-app pager / card preview (between thumb and master). */
export const PREVIEW_MAX_EDGE = 1600;
export const PREVIEW_JPEG_QUALITY = 0.9;
