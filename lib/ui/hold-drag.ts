/** Hold duration before lift-and-drag (ms). */
export const HOLD_DRAG_MS = 500;

/** Still press on subject card → delete confirm (ms). */
export const DELETE_STILL_HOLD_MS = 1000;

/** Shorter hold when triple-tap merge is armed (ms). */
export const MERGE_HOLD_DRAG_MS = 320;

/** Max gap between taps for triple-tap merge (ms). */
export const TRIPLE_TAP_MERGE_MS = 700;
